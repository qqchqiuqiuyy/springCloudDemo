server:
  port: 3344
  
  
spring:
  application:
    name: MicroServiceCloud-config
  cloud: 
    config:
      server:
        git:
          uri: git@github.com:qqchqiuqiuyy/microservicecloud-config.git # github仓库名
          

