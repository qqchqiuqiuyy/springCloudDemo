spring:
  cloud:
    config:
      name: microservicecloud-config-client # 需要从github上读取资源名称 注意
      profile: test # 本次访问的配置项
      label: master
      uri: http://config-3344.com:3344 # 本微服务启动先去找3344号服务,通过springcloudconfig获取github服务地址
      
